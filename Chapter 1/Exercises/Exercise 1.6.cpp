/*
The program is illegal; as it contains semicolon at each line which will terminate the program after printing the sum of v1. To edit it:
*/
#include<bits/stdc++.h>
using namespace std;

int main()
{
    int v1, v2;
    cin >> v1 >> v2;
    cout << "The sum of " << v1 << " and " << v2 << " is " << v1 + v2 << endl;
    return 0;
}
